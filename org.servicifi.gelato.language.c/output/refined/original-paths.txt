!flag->printf("No Record Found!!\n")
empId == emps[i].empId->printf("Enter salary raise for the employee (in percentage):")
num->int num
break;->!flag->printf("No Record Found!!\n")
!ch->break;->0
break;->fclose(outfile)
1->scanf("%d", &empId)
->main()->scanf("%d", &num)
strcmp(input.empId,e.empId)->fseek(outfile,-sizeof(struct employee),SEEK_CUR)
infile == NULL->fprintf(stderr, "\nError opening accounts.dat\n\n")
e->struct employee e
readEmployeeDetails(int num)->i=0
fread (input, sizeof(struct employee), 1, infile)->es[i++] = input;
fread(input,sizeof(input),1,outfile)->strcmp(input.empId,e.empId)->break;->fclose(outfile)
e.basic > SALARY_TAX_THRESHOLD->taxes = (e.gross - SALARY_TAX_THRESHOLD) * HIGH_RATE + SALARY_TAX_THRESHOLD * LOW_RATE ;
outfile == NULL->fprintf(stderr, "\nError opening accounts.dat\n\n")
readEmployeeDetails->readEmployeeDetails(int num)->->
updateEmployeeDetails(struct employee e)->outfile == NULL->fprintf(stderr, "\nError opening accounts.dat\n\n")
->->main()->scanf("%d", &num)
i < num->i++
updateEmployeeDetails->e->struct employee e
